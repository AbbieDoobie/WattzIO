;Do not change settings here, this is just the display default MCM uses on a fresh install.
;Change settings within MCM, it writes to /data/MCM/settings/WIO-InputSwapper.ini itself.

[Settings]
iInputPreference=0
iHysteresisSeconds=2
iLookInputSource=0
bStopAutoMoveWithStick=0
bVerboseDiagnostics=0
