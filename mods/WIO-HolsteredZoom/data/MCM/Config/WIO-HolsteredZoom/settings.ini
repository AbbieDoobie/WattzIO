;Do not change settings here, this is just the display default MCM uses on a fresh install.
;Change settings within MCM, it writes to /data/MCM/settings/WIO-HolsteredZoom.ini itself.

[Settings]
bEnabled=1
iZoomPercent=75
iZoomSpeedMs=100
bApplyInFirstPerson=1
bApplyInThirdPerson=1
bAnalogTriggerScaling=0
iHudRevealMode=0
